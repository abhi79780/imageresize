import Link from 'next/link';
const tools=[['Image Compressor','Reduce JPG, PNG and WebP file size in your browser.','/image-compressor'],['Image Resizer','Change image dimensions while preserving the original file.','/image-resizer'],['JPG to PNG','Convert photographs and graphics to PNG.','/jpg-to-png'],['PNG to JPG','Create compact JPG files with a white background.','/png-to-jpg'],['WebP to JPG','Convert WebP images for broader compatibility.','/webp-to-jpg'],['Image Cropper','Crop and prepare images for the exact frame you need.','/image-cropper'],['Image to Base64','Turn an image into a local Base64 data string.','/image-to-base64'],['Favicon Generator','Prepare common favicon and app icon sizes.','/favicon-generator'],['Background Remover','Remove backgrounds locally and edit the result.','/background-remover']];
export const metadata={title:'Free Image Tools',description:'Browse free browser-based image tools for resizing, compression, conversion, cropping and background removal.'};
export default function Tools(){return <main className="toolPage"><div className="container"><div className="crumb"><Link href="/">Home</Link> <span>›</span> Tools</div><h1>Free Image Tools</h1><p className="muted">Simple image utilities for everyday editing, conversion and optimization. No account is required.</p><div className="toolsGrid" style={{marginTop:30}}>{tools.map(([name,description,url])=><Link className="toolCard" href={url} key={url}><div className="toolIcon">✦</div><h3>{name}</h3><p>{description}</p><button className="cardArrow" aria-label={'Open '+name}>→</button></Link>)}</div><div className="content"><h2>Choose the right tool</h2><p>Use the compressor when file weight matters, the resizer when dimensions need to change, and a converter when a website or application expects a particular format. All tools are designed to keep the workflow understandable and local where the browser supports it.</p></div></div></main>}
